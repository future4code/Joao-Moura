### Exercício 4
a) comando: 
```sql
SELECT MAX(salary) from Actor;
```
b) comando: 
```sql
SELECT MAX(salary) from Actor;
```
c) comando: 
```sql
SELECT COUNT(*) from Actor
WHERE gender = "female";
```
d) comando: 
```sql
SELECT SUM(salary) from Actor;
```